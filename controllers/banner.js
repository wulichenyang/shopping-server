class BannerController {

  // 添加banner
  static async uploadBanner(ctx) {
    // await ……
  }

  // 获取banner列表
  static async getBannerList(ctx) {
    // await ……
  }

  // 获取banner详细信息
  static async getBannerDetail(ctx) {
    // await ……
  }

  // 删除banner
  static async deleteBanner(ctx) {
    // await ……
  }

  // 更新banner
  static async updateBanner(ctx) {
    // await ……
  }

}

module.exports = BannerController;