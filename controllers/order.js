class OrderController {

  // 添加Order
  static async addOrder(ctx) {
    // await ……
  }

  // 获取Order，并返回每个goods具体信息
  static async getOrder(ctx) {
    // await ……
  }

  // 删除Order
  static async deleteOrder(ctx) {
    // await ……
  }

  // 更新Order
  static async updateOrder(ctx) {
    // await ……
  }

}

module.exports = OrderController;